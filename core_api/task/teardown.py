def teardown_environment(
    *,
    cookies: dict,
    headers: dict,
    query_params: dict,
    path_params: dict,
    body: dict,
    timeout: int = 30,
    retry_count: int = 3,
    **kwargs,
) -> None:
    """Destroy the infrastructure associated with the release."""
    pass
