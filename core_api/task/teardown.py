def teardown_environment(
    *,
    cookies: dict = None,
    headers: dict = None,
    query_params: dict = None,
    path_params: dict = None,
    body: dict = None,
    timeout: int = 30,
    retry_count: int = 3,
    **kwargs,
) -> None:
    """Destroy the infrastructure associated with the release."""
    pass
